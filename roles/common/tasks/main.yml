---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install essential administration tools
  ansible.builtin.apt:
    name:
      - git
      - curl
      - vim
    state: present
